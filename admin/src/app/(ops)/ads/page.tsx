import { OpsEmptyState } from "@/components/shell/OpsEmptyState";

export default function AdsPage() {
  return (
    <OpsEmptyState
      title="Ads"
      description="Ad unit IDs, daily caps (rewarded / interstitial), placement toggles for challenge and tools. No gift-for-ad loops."
    />
  );
}
